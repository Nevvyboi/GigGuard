# Copy this file to backend/.env and fill in your own values.
# The real .env is gitignored. Never commit secrets.

# Investec programmable banking sandbox credentials.
# The sandbox uses one shared, public set of credentials that Investec
# publishes in their developer docs, so these are safe to use as is for the
# sandbox. For your own profile, swap in the values from the Investec
# developer portal. The backend uses them to fetch your account transactions.
INVESTEC_CLIENT_ID=yAxzQRFX97vOcyQAwluEU6H6ePxMA5eY
INVESTEC_SECRET=4dY0PjEYqoBrZ99r
INVESTEC_API_KEY=eUF4elFSRlg5N3ZPY3lRQXdsdUVVNkg2ZVB4TUE1ZVk6YVc1MlpYTjBaV04wWlcxRmRHaGpHUkJ0WVdOamIzVnVaSE50WTJGdVkwdDJlQT09

# The account to poll for income. The sandbox "Mr Smith" account works out of
# the box. Leave blank to set it later with a /setup call.
INVESTEC_ACCOUNT_ID=3353431574710163189587446

# Shared secret between the card code and this backend. The card sends it
# as the x-api-key header on every /check and /record call. Make it long
# and random. The same value goes into the card IDE as GIGGUARD_API_KEY.
GIGGUARD_API_KEY=change-me-to-a-long-random-string

# Port the backend listens on. Kept at 3000 so the dashboards and the card
# IDE webhook URL work without being told otherwise.
PORT=3000

# Where the driver ledgers are persisted. Gitignored, created on first run.
# STORE_PATH=backend/data/store.json

# Which origins may call the API from a browser. The dashboards are opened
# straight off the filesystem, so the default is wide open. Narrow this to a
# comma separated list of real origins before any non local use.
# CORS_ORIGINS=*

# Optional overrides. Default to the Investec sandbox. Point these at the live
# hosts (identity.secure.investec.com/connect/token and openapi.investec.com)
# only when you are ready for production.
# INVESTEC_TOKEN_URL=https://openapisandbox.investec.com/identity/v2/oauth2/token
# INVESTEC_API_BASE=https://openapisandbox.investec.com/za/pb/v1
