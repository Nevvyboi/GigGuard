# dependencies
node_modules/

# real secrets, never commit these
.env
backend/.env
*.local

# persisted store (holds driver ledgers and Investec creds), never commit
backend/data/
data/store.json

# logs and os cruft
*.log
npm-debug.log*
.DS_Store

# editor bits
.vscode/
.idea/
